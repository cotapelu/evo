#!/usr/bin/env node
/**
 * Todos Renderer
 *
 * Beautiful UI for todo tool results.
 * Shows phases, tasks with status icons, progress bar.
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
/**
 * Register the todos renderer.
 */
export declare function registerTodosRenderer(api: ExtensionAPI): void;
//# sourceMappingURL=todos-renderer.d.ts.map