#!/usr/bin/env node
/**
 * Compaction Hook
 *
 * Listens to session_before_compact to customize compaction summaries.
 * Demonstrates: api.on("session_before_compact", handler)
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
export declare function registerCompactionHook(api: ExtensionAPI): void;
//# sourceMappingURL=compaction-hook.d.ts.map