#!/usr/bin/env node
/**
 * Cancel Command
 *
 * Slash command `/cancel` aborts the current running operation
 * (tool execution or LLM generation) via the context's abort signal.
 */
import type { ExtensionAPI } from '@earendil-works/pi-coding-agent';
export default function cancelCommand(api: ExtensionAPI): void;
//# sourceMappingURL=cancel-command.d.ts.map